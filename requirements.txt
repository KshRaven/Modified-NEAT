# Core dependencies (CPU-compatible, always installed)
numpy
numba
matplotlib
tensorboard
torch
gymnasium
pygame

# GPU/CUDA dependencies (optional, install via: pip install -e .[gpu] or python install.py)
# Only needed if you have NVIDIA GPU and want GPU acceleration
# cuda-python
# cuda-toolkit
# numba-cuda[cu13]
# cupy-cuda13x